#include <stdio.h>

int main() {
    int i, n; 
    float num [], sum 0;avg;
    

    printf("enter the number 5 integers:\n");
    for (i = 0; i < 5; i++) {
        printf("Enter element %d: ", i + 1);
        scanf("%d", &arr[i]); 
        sum += arr[i]; 
    }

    average = (float)sum / 5; 

    printf("Average = %.1f\n", average);

    return 0;
}
